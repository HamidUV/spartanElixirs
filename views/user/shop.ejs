<%- include('../layout/userLayout/userHeader.ejs') %>
<div class="bg-light py-3">
    <div class="container">
      <div class="row">
        <div class="col-md-12 mb-0"><a href="/">Home</a> <span class="mx-2 mb-0">/</span> <strong class="text-black">Shop</strong></div>
      </div>
    </div>
  </div>

  <div class="site-section">
    <div class="container">

      <div class="row mb-5">
        <div class="col-md-12 order-2">

          <div class="row">
            <div class="col-md-12 mb-5">
              <div class="float-md-left mb-4"><h2 class="text-black h5">Shop All</h2></div>
              <div class="d-flex">
                <div class="dropdown mr-1 ml-md-auto">
                  <button type="button" class="btn btn-secondary btn-sm dropdown-toggle" id="dropdownMenuOffset" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Category
                  </button>
                  <div class="dropdown-menu" aria-labelledby="dropdownMenuOffset">
                    <% categoryData.forEach(category => { %>
                    <a class="dropdown-item" href="/category/<%= category.name %>"><%= category.name %></a>
                    <% }); %>
                  </div>
                </div>
                <div class="btn-group">
                  <button type="button" class="btn btn-secondary btn-sm dropdown-toggle" id="dropdownMenuReference" data-toggle="dropdown">Price</button>
                  <div class="dropdown-menu" aria-labelledby="dropdownMenuReference">
                   
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="/sort/low-to-high" id="low-to-high">Low to High</a>
                    <a class="dropdown-item" href="/sort/high-to-low" id="high-to-low">High to Low</a>
                    
                  </div>
                </div>
              </div>
            </div>
          </div>

          

          <div class="row mb-5 ">
            <% productData.forEach((product) => { %>
            <div class="col-sm-6 col-lg-4 mb-4 " data-aos="fade-up">
              <div class="block-4 text-center border">
                <figure class="block-4-image">
                  <a href="/single-item/<%= product.name %>">
                    <img class="product-image"  src="/images/<%=product.image %>" alt="<%= product.name %>">
                </figure>

                <div class="block-4-text p-4">
                  <h3><a > <%=product.name %></a></h3>
                  <p class="mb-0"><%=product.category %></p>
                  <p class="text-primary font-weight-bold">₹<%=product.price %></p>
                </div>
              </div>
            </div>
            <% }); %>
          </div>
         

          <div class="row" data-aos="fade-up">
            <div class="col-md-12 text-center">
              <div class="site-block-27">
                <ul>
                  <% if (currentPage > 1) { %>
                    <li>
                      <a href="?page=<%= currentPage - 1 %>&search=<%= search %>"><</a>
                    </li>
                  <% } %>
          
                  <% for (let j = 1; j <= totalPages; j++) { %>
                    <li class="<%= currentPage === j ? 'active' : '' %>">                
                      <a href="?page=<%= j %>&search=<%= search %>"><%= j %></a>
                    </li>
                  <% } %>
                  <% if (currentPage < totalPages) { %>
                    <li>
                      <a href="?page=<%= currentPage + 1 %>&search=<%= search %>">></a>
                      
                    </li>
                    
                  <% } %>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </div>

      
      
    </div>
  </div>

  <style>
    .product-image{
      width: 16rem;
      height: 20rem;
    }
  </style>
  <%- include('../layout/userLayout/userFooter.ejs') %>